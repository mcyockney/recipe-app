    <div class="row">
        <div class="well col-md-8 col-md-offset-2">
            <%= form_for @review, url: recipe_reviews_path do |f| %>
            <%= f.label :review_entry %>
            <%= f.text_field :review_entry %>
            <%= f.submit(@review.new_record? ? 'Add Review' : 'Edit Review', class: "btn btn-success") %>
            <% end %>
        </div>
    </div>
